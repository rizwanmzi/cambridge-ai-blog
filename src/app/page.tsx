export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function Home() {
  // Authenticated users see DashboardShell (via DashboardLayout)
  // Unauthenticated users are redirected to /login (via DashboardLayout)
  return null;
}
